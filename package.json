{
  "name": "fukict",
  "version": "0.1.9",
  "private": true,
  "description": "专注于性能场景的轻量级 DOM 渲染库",
  "keywords": [
    "dom",
    "jsx",
    "vanilla",
    "typescript",
    "rendering",
    "performance"
  ],
  "homepage": "https://github.com/fukict/fukict#readme",
  "bugs": {
    "url": "https://github.com/fukict/fukict/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fukict/fukict.git"
  },
  "license": "MIT",
  "author": "xiaoyown <xiaoyown@example.com>",
  "type": "module",
  "scripts": {
    "build": "pnpm clean && pnpm extract-metadata && tsx scripts/build-package.ts --all --no-watch",
    "changeset": "changeset",
    "changeset:alpha": "changeset version --snapshot alpha",
    "changeset:beta": "changeset version --snapshot beta",
    "changeset:check": "tsx scripts/check-changeset.ts",
    "changeset:publish": "changeset publish --no-git-tag",
    "changeset:release": "pnpm build && changeset publish --no-git-tag",
    "changeset:version": "changeset version",
    "clean": "premove packages/*/dist",
    "clear": "premove **/node_modules && premove pnpm-lock.yaml",
    "commit": "tsx scripts/commit.ts",
    "dev:no-build": "npx http-server -p 8080 -c-1 --cors",
    "dev:pkgs": "tsx scripts/build-package.ts --all --watch",
    "extract-metadata": "tsx scripts/extract-metadata.ts",
    "format": "prettier --write .",
    "lint": "pnpm lint:types && eslint .",
    "lint:fix": "eslint . --fix",
    "lint:types": "pnpm -r --parallel run lint",
    "prepare": "husky",
    "publish": "tsx scripts/publish.ts",
    "sync:version": "tsx scripts/sync-version.ts",
    "tag": "tsx scripts/tag.ts",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "version:alpha": "changeset version --snapshot alpha && changeset publish --tag alpha --no-git-tag",
    "version:beta": "changeset version --snapshot beta && changeset publish --tag beta --no-git-tag",
    "version:rc": "changeset version --snapshot rc && changeset publish --tag rc --no-git-tag"
  },
  "config": {
    "commitizen": {
      "path": "@commitlint/cz-commitlint"
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@commitlint/cz-commitlint": "^20.1.0",
    "@eslint/js": "^9.28.0",
    "@trivago/prettier-plugin-sort-imports": "5.2.2",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.33.1",
    "@typescript-eslint/parser": "^8.33.1",
    "commitizen": "^4.3.0",
    "eslint": "^9.28.0",
    "eslint-config-prettier": "^10.1.5",
    "globals": "^16.4.0",
    "husky": "^9.1.7",
    "premove": "^4.0.0",
    "prettier": "3.4.2",
    "prettier-plugin-packagejson": "2.5.6",
    "prettier-plugin-sort-json": "4.0.0",
    "prettier-plugin-tailwindcss": "^0.7.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "yaml": "^2.6.1"
  },
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0"
  }
}
