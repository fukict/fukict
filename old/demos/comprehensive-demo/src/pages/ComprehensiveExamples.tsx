import { RouteWidget, RouterView } from '@fukict/router';

export class ComprehensiveExamples extends RouteWidget {
  render() {
    return <RouterView router={this.router} />;
  }
}

export default ComprehensiveExamples;
