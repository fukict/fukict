/** Global hook name injected into page window */
export const HOOK_NAME = '__FUKICT_DEVTOOLS_HOOK__';

/** Flag to check if Fukict is detected */
export const DETECTED_FLAG = '__FUKICT_DETECTED__';

/** DevTools connection port name */
export const PORT_NAME = 'fukict-devtools-panel';
